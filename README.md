# ssl-auto-trust
Spring Boot Auto-Configuration library that automates the trust of corporate proxy TLS certificates (e.g., ZScaler) to resolve PKIX path building errors. Requires explicit opt-in via `ssl.auto-trust.enabled=true`. Supports Java 8+ and Spring Boot 2.x+.
